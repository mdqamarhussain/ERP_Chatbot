# Placeholder for document classification logic
def classify_document(text):
    # Simple placeholder function for document classification
    return "invoice" if "invoice" in text.lower() else "other"
